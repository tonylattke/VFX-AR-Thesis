# Dependencies

# platform :ios, '11.0'

target 'VFX-AR' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  # Pods for VFX-AR
  pod 'LASwift', '~> 0.1.0'
  pod 'FontAwesome.swift'
  pod 'SwiftyJSON'
  pod 'ARCL'

  target 'VFX-ARTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'VFX-ARUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
